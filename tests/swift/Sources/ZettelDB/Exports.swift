// Re-export the generated UniFFI bindings.
// After building the XCFramework, copy out/swift/zdb_core.swift here.
//
// The generated file imports the C FFI module (zdb_coreFFI) provided by
// the XCFramework binary target and exposes the Swift API (ZettelDriver, etc.).
